import React from "react";

const Nav = () => {

    return(
        <nav>
            <h1>Where in the world?</h1>
            <button className="mode">MODE</button>
        </nav>
    );
}

export default Nav;